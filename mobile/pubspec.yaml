name: mattioli_os
description: "Evolve — build better habits and reach your goals, with a privacy-first encrypted offline mode."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.1.3+39

environment:
  sdk: ^3.11.5

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.6.0
  flutter_riverpod: ^3.3.1

  go_router: ^17.2.2
  google_fonts: ^8.0.2
  lucide_icons_flutter: ^3.1.13
  fl_chart: ^0.69.0
  shared_preferences: ^2.5.5
  share_plus: ^12.0.2
  flutter_colorpicker: ^1.1.0
  image_picker: ^1.2.1
  flutter_local_notifications: ^21.0.0
  timezone: ^0.11.0
  flutter_timezone: ^5.0.2
  local_auth: ^3.0.1
  supabase_flutter: ^2.8.3
  google_sign_in: ^6.2.1
  sign_in_with_apple: ^8.1.0
  crypto: ^3.0.7
  # Shared E2E-encrypted CloudKit sync core (engine, AES-GCM crypto, private-DB
  # schema, sync service) — single source of truth with the macOS desktop app.
  evolve_sync:
    path: ../packages/evolve_sync
  # Shared pure-Dart core for auto-verified habits (template catalog, rule model,
  # reconcile/verdict engine, HealthKit + Screen Time bridge contracts).
  evolve_verification:
    path: ../packages/evolve_verification
  # The public legal + support URLs, defined once for both apps. These are App
  # Store compliance surfaces (Guidelines 1.5 and 3.1.2), not ordinary links.
  evolve_legal:
    path: ../packages/evolve_legal
  intl: ^0.20.2
  permission_handler: ^12.0.1
  tutorial_coach_mark: ^1.3.3
  sentry_flutter: ^9.20.0
  flutter_secure_storage: ^9.2.2
  url_launcher: ^6.3.2
  flutter_markdown: ^0.7.7+1
  purchases_flutter: ^10.1.0
  purchases_ui_flutter: ^10.1.0
  sqflite_sqlcipher: ^3.4.0
  path_provider: ^2.1.6
  path: ^1.9.1
  uuid: ^4.5.3
  # App version metadata for Sentry release/dist (SEC-3). Already in the tree
  # transitively via sentry_flutter; pinned here as a direct dependency.
  package_info_plus: ^9.0.1

  # Localization (type-safe, nested, context-free `t`). Replaces the legacy
  # gen_l10n `translate()` shim — see LOCALIZATION_PLAN.md.
  # NOTE: version ranges were not resolvable offline; run `flutter pub get`
  # (or `flutter pub add slang slang_flutter dev:slang_build_runner`) to pin
  # the current compatible versions.
  slang: ^4.0.0
  slang_flutter: ^4.0.0
  archive: ^4.0.9
  file_picker: ^11.0.2
  markdown: ^7.3.1
  crop_image: ^1.0.17

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Transitive dep of flutter_local_notifications; declared directly so tests
  # may use MockPlatformInterfaceMixin to stub the notifications platform.
  plugin_platform_interface: ^2.1.8

  # In-memory FFI SQLite for unit-testing the Private DB schema + migrations
  # (PrivateDbSchema) without a device. Encryption (SQLCipher) is orthogonal to
  # schema correctness, so these tests open an unencrypted FFI database.
  sqflite_common_ffi: ^2.3.3

  slang_build_runner: ^4.0.0

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

dependency_overrides:
  path_provider_foundation: 2.5.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/app_icon.png"
  remove_alpha_ios: true
